export enum FieldTypes {
  TEXT_INPUT = 'text',
  NUMBER_INPUT = 'number',
  TEXTAREA = 'textarea',
  SELECT = 'select',
  DATE = 'date',
  USER_SELECT = 'user-select',
  MULTI_USER_SELECT = 'multi-user-select',
}
